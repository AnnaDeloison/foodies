  <div class="navbar navbar-expand-sm navbar-light navbar-lewagon">
    <%= link_to root_path, class: "navbar-brand logo " do %>
    <%= image_tag "logoV2.png" %>
      <% end %>

    <div class="menu-central">
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
          <li class="nav-item ">
            <a  href="#">Histoire</a>
          </li>
          <li class="nav-item ">
            <%= link_to "Recettes", recipes_path  %>
<!--             <a class="" href="./recipes">Recettes</a> -->
          </li>
          <li class="nav-item dropdown transition">
            <a class="dropdown-toggle" id="navbarDropdown" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false" href="" >Troubles</a>


            <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
              <%= link_to "Chimiothérapie", trouble_path(Trouble.where("name ILIKE ?", "Chimiothérapie")[0].id), class: "dropdown-item" %>
              <%= link_to "Digestion", trouble_path(Trouble.where("name ILIKE ?", "Digestion")[0].id), class: "dropdown-item" %>
              <%= link_to "Déglutition", trouble_path(Trouble.where("name ILIKE ?", "Déglutition")[0].id), class: "dropdown-item" %>
            </div>
          </li>
        </ul>
      </div>
    </div>



<div class="connexion">
        <ul class="nav-item dropdown position-panier">

    <% @cart = 0 %>
    <% @badge = 0 %>
    <% unless session[:cart].blank? %>
    <% session[:cart].each do |element| %>
    <% @cart += element["price_cents"] %>
    <% @badge += 1 %>
    <% end %>
    <% end %>


  <a class="dropdown-toggle nav-item panier" id="navbarDropdown" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false" href="" >Panier <span class="badge"> <%= @badge %></span></a>
             <div class="dropdown-menu dropdown-menu-right" id="panier" aria-labelledby="navbarDropdown">

  <div class="container-panier">
  <div class="shopping-cart">
    <div class="shopping-cart-header">
      <i class="fa fa-shopping-cart cart-icon"></i><span class="badge"><%= @badge %></span>
      <div class="shopping-cart-total">
        <span class="lighter-text">Total:</span>
        <span class="main-color-text"><%= @cart %>€</span>
      </div>
    </div> <!--end shopping-cart-header -->




<ul class="shopping-cart-items" id="transition">
      <% if session[:cart].blank? %>
    <p>Panier vide</p>
    <% else %>
    <% id = 0 %>
    <% session[:cart].each do |element|  %>
      <li class="clearfix">
       <%= link_to recipe_url(session[:cart][id]["id"]) do  %>
       <img  class="photo-cart" src= <%= element["photo_url"] %> alt="item1" />
        <% id = id + 1 %>
        <% end %>
        <span class="item-name"> <%= element["name"] %> <%= element["price_cents"] %>€</span>
<!--         <span class="item-price"><%= element["price_cents"] %>€</span> -->
        <span class="item-quantity"> <%= link_to "supprimer", delete_recipe_path(element), id: "panier-supprimer" %></span>
      </li>
          <% end %>
       <% end %>

   <%= link_to "Valider et commander", new_order_path, class: "button-cart btn btn primary", id: "valider" %>
</ul>
</div>
</div>
</div>
      <% if user_signed_in? %>
          <li class="nav-item dropdown">
            <%= image_tag "https://media-exp1.licdn.com/dms/image/C5603AQHvPAOmAt9jMg/profile-displayphoto-shrink_200_200/0?e=1585180800&v=beta&t=gnf-hifQs9ddzieqnbecwLZTBGWWpyoH3AGhM0qIGSo",
                        width: 40,
                        height: 40,
                        crop: :fill,
                        class: "avatar-picture dropdown-toggle image",
                        id: "navbarDropdown", data: { toggle: "dropdown" },
                        'aria-haspopup': true,
                        'aria-expanded': false %>


            <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
              <%= link_to "Profil", profile_path, class: "dropdown-item" %>
              <%= link_to "Another action", "#", class: "dropdown-item" %>
              <%= link_to "Log out", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
            </div>
          </li>
      <% else %>
          <li class="nav-item">
            <%= link_to "Login", new_user_session_path, class: "dropdown-item" %>
          </li>
      <% end %>
    </div>
  </div>
</div>

